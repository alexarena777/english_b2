import { BookMarked } from "lucide-react";
export function GrammarExplanation({ title, short, details, examples, mistakes }: { title: string; short: string; details: string; examples: string[]; mistakes: string[] }) { return <div className="grammar-explanation"><div className="explanation-lead"><BookMarked/><div><h2>{title}</h2><p>{short}</p></div></div><div className="explanation-grid"><section><h3>Come funziona</h3><p>{details}</p><h3>Esempi</h3>{examples.map((example) => <p className="grammar-example" key={example}>{example}</p>)}</section><aside><h3>Errori comuni degli italiani</h3><ul>{mistakes.map((mistake) => <li key={mistake}>{mistake}</li>)}</ul></aside></div></div>; }
